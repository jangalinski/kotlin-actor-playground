# Playing with kotlin actors and coroutines


## step by step

* init gradle with `gradle wrapper`, make sure the latest version is used in `gradle/wrapper/gradle-wrapper.properties`
* `git init` and `echo .gradle/ > .gitignore`
